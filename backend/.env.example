# SMTP config for OTP emails
# Example using Gmail (use App Password or SMTP relay):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-smtp-password-or-app-password
# FROM_EMAIL=your-email@gmail.com

# Note: If you don't provide SMTP settings the server will print OTPs to the console for local development.
